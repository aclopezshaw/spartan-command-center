# System status

**Document status:** Living implementation inventory  
**Last verified:** 2026-07-27
**Implementation authority:** Repository code

This document distinguishes current implementation from intended behavior. “Implemented” means a code path exists; it does not mean the path is secure, fully tested, or free of defects.

## Technology baseline

| Area | Verified state | Evidence |
| --- | --- | --- |
| Framework | Implemented with Next.js 16.2.9 App Router and React 19.2.4. | `dependencies` in [`package.json`](../package.json) |
| Styling | Tailwind CSS 4 through PostCSS, with global theme variables. | [`postcss.config.mjs`](../postcss.config.mjs), [`src/app/globals.css`](../src/app/globals.css) |
| Operational storage | Notion SDK is used by server pages, shared libraries, and Route Handlers through one lazy, server-only client accessor. | `getNotionClient` in [`src/lib/notion-client.ts`](../src/lib/notion-client.ts); `getTodaySitrep`, `getHydrationTotalForOperationalDay`, `createServiceHistoryEntry`, and related functions in [`src/lib/notion.ts`](../src/lib/notion.ts) |
| Authentication | Implemented for the current single-user scope. Login issues a signed, versioned, seven-day ALEX-225 session; verification rejects forged, malformed, tampered, wrong-secret, expired, and future-dated tokens. Production requires a dedicated `SESSION_SECRET`. | `createSessionToken` and `verifySessionToken` in [`src/lib/session-token.ts`](../src/lib/session-token.ts), `hasAuthorizedSession` in [`src/lib/auth.ts`](../src/lib/auth.ts), login/logout Route Handlers, and [ADR-0008](adr/0008-signed-single-user-sessions.md) |
| Automated tests | Implemented as a v0.6 release baseline. The Node harness covers signed sessions and route authorization, Denver calendar/DST boundaries, campaign and Fireteam progression, achievement rules, and Service History property contracts. Browser workflows and live Notion schema/permission behavior still require proportionate smoke tests. | `test`, `test:campaign`, `test:foundation`, and `validate:release` in [`package.json`](../package.json); suites under [`tests`](../tests); [`QUALITY_GATES.md`](QUALITY_GATES.md) |
| Production build | Implemented as a required local and CI release gate. | `build` and `validate:release` in [`package.json`](../package.json); [`.github/workflows/quality-gates.yml`](../.github/workflows/quality-gates.yml) |
| Lint baseline | Implemented as an explicit ratchet over technical debt. Raw ESLint still reports 45 errors and 12 warnings; the release gate fails if totals or any file/rule/severity bucket increase. | `lint` and `lint:ratchet` in [`package.json`](../package.json), [`scripts/check-lint-baseline.mjs`](../scripts/check-lint-baseline.mjs), and [`scripts/lint-baseline.json`](../scripts/lint-baseline.json) |

## Product surfaces

| Surface | Status | Verified implementation | Known gaps |
| --- | --- | --- | --- |
| Login and protected routes | Implemented for the current single-user scope | `LoginPage`, login/logout Route Handlers, `ProtectedLayout`, and all private Route Handlers use the centralized signed-session contract. The legacy literal cookie is deleted and never accepted. Mutation routes retain domain-specific payload validation and Notion-record ownership checks where a client supplies a page ID. | Rate limiting, credential lockout, device-level revocation, and multi-user identity are not implemented. These are outside the present single-user ticket scope. |
| Command HUD | Partially implemented | `CommandHudPage` loads the Service Record, current SITREP, weekly operations, workout count, event system, and authoritative Fireteam Assignment state. The zero-reward Fireteam Assignment Ceremonial Event appears in the existing right-side event slot when no Campaign Event is active, links to the Assembly Hall, survives interrupted/finalizing states, and disappears only after assignment completion verifies. Campaign day, Sunday week start, and time-sensitive background selection use the shared America/Denver operational-time helpers. `getHudBackground` preserves the existing Phase I rotation and activates the seven ordinary Phase II plates—morning berth, classroom, mess hall, post-PT turnover, evening study, chess/night prep, and lights-out bunk view—only when Campaign Operations reports Phase II. Shared Daily SITREP and Weekly Operations controls optimistically toggle, refresh only after a confirmed write, and restore the previous state with an accessible retry message after HTTP or network failure. The inactive 100% Shield placeholder was removed under [SDCB #238](https://app.notion.com/39ebc7d80f458111b201effe3eac3788); no unsupported health replacement is rendered. | Promotion eligibility does not yet emit a Ceremonial Event; hardcoded campaign start and hardcoded readiness copy remain. |
| Daily SITREP | Implemented with technical debt | `getTodaySitrep` retrieves or creates the Denver-dated record; the authenticated mutation route allowlists current habit properties, persists checkboxes, and reconciles eligible Unit Cohesion operations. | Concurrent record creation is not guarded; mobile and hydration entry points retain broader authorization debt. |
| Weekly Operations | Implemented with technical debt | `getOrCreateWeeklyOperations` retrieves or creates the current Denver-dated Sunday-start record, links new rows to ALEX-225 through the canonical `🪖 Service Record` relation, and repairs an empty relation on an existing current row. The authenticated mutation route allowlists Workouts, Shot, and Planning, verifies the supplied row is current, and reconciles eligible Unit Cohesion operations. | Concurrent creation is not guarded; a non-empty relation to an unexpected Service Record blocks rather than being overwritten; the Sunday convention differs from the academic pipeline's explicit Monday convention. |
| Service Record | Partially implemented | `Home` in the Service Record page retrieves Notion data and earned achievements. The Current Campaign panel calculates exact phase-scoped Daily SITREP, Weekly Operations, and linked event-history XP, shows that auditable breakdown against the expanded phase maximum, and projects medal pace without re-adding event XP already earned. Color-matched XP values sit beneath the Bronze, Silver, and Gold progress-bar checkpoints until earned XP exceeds each checkpoint. The lifetime Service Score remains the authority for Current Rank. The current-rank panel presents the Bronze I insignia between the Recruit and Bronze I labels. The right rail contains the designation portrait and an honest Campaign Medals empty state. `CampaignMedalCard` provides reusable Bronze, Silver, and Gold card treatments backed by transparent production artwork, and `sortCampaignMedalsNewestFirst` defines newest-first display order. | Rank labels and displayed insignia are hardcoded rather than calculated from authoritative XP; portrait controls are hardcoded; frozen Campaign Operations medal results are not yet queried into Campaign Medal cards; calculated readiness is overwritten by achievement totals. |
| Rank insignia | Partially implemented | All 38 approved transparent production assets exist under `public/images/ranks/`: singular Recruit, Bronze through Onyx Tier I–VI, and singular Champion. `PageHeader` displays Recruit beside the SCP emblem with a cyan glow. The approved progression grammar and external records are documented in [`RANK_INSIGNIA_GUIDE.md`](RANK_INSIGNIA_GUIDE.md). | Rank selection is not yet connected to authoritative XP or promotion logic. |
| Fireteam and command identity | Partially implemented | Transparent production assets exist for Fireteam Epsilon at `public/images/fireteam/fireteam-epsilon-patch.png` and Athena Command at `public/images/command/athena-command-patch.png`. The Phase II Tactical Field Uniform plate at `public/images/uniforms/scp-tactical-field-uniform-guide.png` defines SCP-left / Epsilon-right shoulder placement. The canonical five-member portrait set exists under `public/images/fireteam/dossiers/`, and its shared presentation and event-continuity rules are recorded in [`FIRETEAM_EPSILON_DOSSIERS.md`](FIRETEAM_EPSILON_DOSSIERS.md). The server evaluates and explicitly persists versioned Individual completion eligibility, then owns a non-rerollable Fireteam Epsilon assignment contract containing the permanent identity, five stable member IDs/designations, assignment date, operation/version markers, and initial `Acquaintance I` relationship baselines at `0 / 25`. The Unit Cohesion engine maps every current Daily SITREP and Weekly Operations habit to one teammate affinity, creates or reverses one source-derived zero-XP Service History ledger row only after assignment, derives the approved sixteen-level Acquaintance/Familiar/Trusted/Bonded state, detects duplicate operations, and supplies live values to `/fireteam`. Canonical names, mottos, palettes, symbolism, uniform rules, and the Epsilon-to-Athena progression relationship are recorded in Notion visual guides, the Standard Uniform Manual, and [SDCB #151](https://app.notion.com/p/399bc7d80f45818793cdf215d092b463) / [#152](https://app.notion.com/p/399bc7d80f4581ff82e4d041beef0a72). | Narrative relationship unlocks, Phase III carry-forward presentation, Command assignment, Phase III Athena wear rules, and award transitions remain planned. Notion cannot enforce a distributed unique operation constraint, so cross-process duplicates are detected for reconciliation rather than claimed impossible. |
| Achievements | Partially implemented | `evaluateAchievements` supports Persistence and Discipline for daily objectives plus weekly Workout, Shot, and Plan records; newly awarded achievements create linked Service History entries. | Queries stop after the first Notion page; Classified achievements are not evaluated. [SDCB #195](https://app.notion.com/p/39cbc7d80f4581d5a2acc064481cfe19). |
| Hydration | Partially implemented | Training Reports can write Hydration Log entries, calculate an America/Denver operational-day total, and auto-check Water at 96 ounces using the same DST-safe boundary helper. | Mobile uses different volatile storage. [SDCB #196](https://app.notion.com/p/39cbc7d80f458182b2ffd62b34f1dbe1). |
| Workout reporting | Partially implemented | Training Reports provides a controlled, authenticated workout form that validates and writes Notion Workout Log records. The page reports the authenticated weekly workout count, phase minutes and miles, and phase hydration average, deriving the phase range from the active campaign day and refreshing workout metrics after successful submission. | Phase aggregate queries stop after the first Notion page. |
| Intel Reports | Partially implemented | The authenticated Archive contract paginates every material and partitions Active titles from recommendation-eligible Low/Medium/High Priority and Wishlist titles. Recommendations are the deduplicated union of every eligible Priority Band title plus at most five highest-Fit-Score titles, ordered by Fit Score with a deterministic title tie-break; Active and Complete records are excluded. Active Materials include the latest Book-linked Reading Report date, or an explicit no-report state. The client renders authoritative Status and optional Priority Band plus explicit loading, empty, and failure states without fabricated cards. Active materials, page progress, and Last Read refresh immediately after a successful report. The shared report contract records an absolute new current page; the server verifies Archive ownership, derives the trusted title and page delta, rejects stale or beyond-total updates, creates a Book-linked Reading Report, and updates Archive progress. Deterministic Book-plus-ending-page identity supports interrupted-write recovery and detected-duplicate blocking. | Notion cannot enforce a distributed uniqueness constraint, so truly concurrent identical first submissions can still race; the multi-step report write remains subject to the transaction constraint in [ADR-0001](adr/0001-notion-as-operational-data-store.md). |
| Academic Operations / Spartan Medical Unit | Partially implemented | `MedicalUnitPage` loads assignment queues, active and up-next Notion quarter records, and the course pipeline. Due Soon and Plan Ahead assignments can be added to the Focus Queue through authenticated `POST /api/smu/orders/focus`; `GET /api/smu/orders` groups authoritative assignment records using Denver-aware due-soon and overdue boundaries; `GET /api/smu/pipeline` calculates Monday-through-Sunday required and optional progress, remaining study minutes, overdue and skipped work, next-exam dates, and high-priority completion; authenticated `GET /api/smu/quarter` returns active and up-next quarter metadata and attempts to resolve related course codes and names when the application integration can access the Courses data source; `POST /api/smu/orders/complete` marks an assignment Complete. The Medical Service Record presents cumulative completed, required, reading, worksheet, and high-priority assignment counts and distinguishes unavailable sources from authoritative zeroes. Reading and worksheet categories are derived from standalone labels in assignment titles. | The current application integration does not expose the Courses relation, so the Next Quarter Outlook course list is empty while quarter metadata remains available. The routes are still named and presented as SMU, authorization is inconsistent, schemas are loosely normalized, and degree progress, graduation, and clinical operations remain hardcoded or disconnected. The domain-neutral Academic Operations contract planned under [SDCB #60](https://app.notion.com/391bc7d80f458060aff4f9b43738f85d) is not complete. |
| Campaign events | Partially implemented | Event scheduling, readiness requirements, retry policy, phase scope, and durable completion state are read from the Notion Events database and its related active Campaign Operations phase. Authenticated status and completion routes use the exact active-phase Notion page identity, server-derived day, sequential order, and authoritative readiness. `getEventOutcomeState` distinguishes upcoming, active, past-due incomplete, failed cooldown, retry-ready, terminal failed, and completed outcomes. A failed review attempts to consume a persisted five-day retry slot that shifts the failed Event and all later Events; it is authorized only when every remaining Event still fits inside the phase, otherwise the failure is terminal. Completion reconciles exactly one XP-bearing Event history before `Defeated`, clears retry availability while preserving consumed schedule slots, and partial retries repair history-only or event-only state while in-process requests coalesce. The terminal panel renders `All Events Complete for This Phase`. The five approved Phase II records and presentation assets are verified at days 5/10/15/20/25 with 250/250/250/250/500 XP. Competitive Phase II completion snapshots readiness, preserves Epsilon's earned 0–4 score, persists deterministic unique rival scores plus cumulative standings and tie-break evidence, and exposes a sanitized authenticated standings status endpoint. `/fireteam/leaderboard` renders the authoritative five-Fireteam board, canonical patches and colors, cumulative points, wins, rank movement, pre-event/active/final states, and newest-first Event outcomes without exposing readiness snapshots, formulas, weights, or seeds. The authenticated rollover contract remains as previously documented. | Frozen medal results are not yet rendered as Campaign Medal cards. Notion cannot enforce distributed uniqueness, so duplicate event histories or standings resolutions block/reconcile rather than being claimed impossible. Database-owned event presentation and reward fields remain separate schema work; durable completion and phase transition remain governed by [#187](https://app.notion.com/p/399bc7d80f4581759845cbb71b982953) and [#49](https://app.notion.com/p/390bc7d80f45804bb7bbe4e1d4d74168). |
| Service History | Partially implemented | The Service History page renders a Notion-backed timeline, including XP and category-colored readiness rewards when present; Notion formulas now generate descriptions for achievement and history records. | Historical backfill, automated records for non-achievement milestones, pagination, and filtering/grouping by campaign or entry type remain incomplete. [SDCB #20](https://app.notion.com/p/390bc7d80f4580ec8976ef5f49cada77), [SDCB #43](https://app.notion.com/p/390bc7d80f45808e9e1ce6d6af9b0e2a) |
| Command Assembly Hall | Partially implemented | `/assembly-hall` replaces the placeholder Promotion Board with a permanent, responsive ceremonial location backed by live Individual completion and Fireteam Assignment contracts. The Fireteam ceremony explicitly freezes the final Phase I record when needed, persists and resumes presentation progress, introduces Epsilon and its roster, writes the canonical assignment before reconciling exactly one zero-reward Assignment Service History record, exposes interrupted-finalization recovery, and offers read-only replay after completion. A guarded Campaign Transition Control exposes read-only rollover evidence and requires an explicit `CONFIRM ROLLOVER` phrase before executing or recovering the authenticated durable transition. The Hall also renders live Service Record rank progress, XP remaining, rank target, threshold status, and a days-to-threshold forecast calculated from the current phase's average earned Daily SITREP habit XP without claiming promotion eligibility. The active/inactive canonical backdrops follow durable assignment state. Navigation uses Assembly Hall and `/promotion-board` permanently redirects. | Authoritative promotion review, summons, transition, and history remain disconnected from ceremonial orders under SDCB #56 and #45. Production deployment and Day 42 authenticated smoke verification remain pending. |
| Armory | Partially implemented | Protected Armory route and placeholder are rendered. | Equipment, unlocks, and rewards are not implemented. |
| Mobile integration | Partially implemented | Mobile HUD GET and objective POST share Daily SITREP functions. | Weekly HUD values are hardcoded; hydration is process memory; intel only echoes/logs the body; endpoints lack authorization. [SDCB #196](https://app.notion.com/p/39cbc7d80f458182b2ffd62b34f1dbe1). |

## Route Handler inventory

All files below expose public HTTP entry points under the App Router. Layouts do not authorize Route Handlers, so every private handler independently calls the shared signed-session verifier. `/api/login` and `/api/logout` are the only public Route Handlers.

| Route | Methods | Status and side effect |
| --- | --- | --- |
| `/api/login` | POST | Validates ALEX-225 credentials and creates the signed, expiring session cookie. |
| `/api/logout` | POST | Deletes current and legacy authentication cookies, then returns to the login page. |
| `/api/complete-event` | POST | Authenticates the session, resolves the exact Event page from the active Notion phase, derives campaign day server-side, validates sequence and readiness, then persists either a retryable Failed review or a recoverable standings/history/event completion. |
| `/api/events/status` | GET | Authenticates the session and returns the active Notion phase, its event schedule, server-derived campaign day, and durable completion IDs. |
| `/api/fireteam/standings` | GET | Authenticates the session and returns sanitized current-phase cumulative standings, event results, and duplicate-resolution warnings without exposing seeds or readiness snapshots. |
| `/api/campaign/rollover` | GET, POST | Authenticates the session. GET reports blocked, ready, recovery, or complete state without mutation. POST freezes and verifies the outgoing phase's exact XP and medal result, creates/reconciles the phase-completion Service History record, completes the outgoing phase, activates the immediate next phase, and re-reads Notion to verify the durable result. |
| `/api/progression/individual-completion` | GET, POST | Authenticates the session. GET read-only evaluates Phase I as Locked, Ready to Finalize, Eligible, or Assigned with evidence. POST explicitly reconciles the versioned status and explanation on the Service Record; it does not freeze XP or create a Fireteam assignment. |
| `/api/progression/fireteam-assignment` | GET, POST | Authenticates the session. GET read-only evaluates locked, available, in-progress, finalizing, completed, or conflict state. POST begins the ceremony, advances one monotonic step, or completes/reconciles the server-owned Epsilon assignment and its exact Assignment history record. |
| `/api/evaluate-achievements` | POST | Authenticates the session and runs achievement evaluation. Partially implemented. |
| `/api/sitrep-checkbox` | POST | Authenticates the session, allowlists current Daily SITREP habits, updates today's record, reconciles eligible Unit Cohesion, and may evaluate achievements. |
| `/api/weekly-operations` | POST | Authenticates the session, allowlists current Weekly Operations habits, verifies the supplied page is the current Sunday-start record, updates it, reconciles eligible Unit Cohesion, and may evaluate achievements. |
| `/api/hydration-log` | POST | Authenticates the session, validates a bounded positive amount, creates a Hydration Log entry, and may check Water. |
| `/api/hydration-total` | GET | Authenticates the session and aggregates the current America/Denver operational day. |
| `/api/hydration-phase-average` | GET | Authenticates the current session and returns average hydration across the active campaign phase. |
| `/api/workout-log` | POST | Authenticates the current session, validates workout fields, and creates a Notion Workout Log record. |
| `/api/workout-summary` | GET | Authenticates the current session and returns minutes and miles across the active campaign phase. |
| `/api/workout-weekly-count` | GET | Authenticates the current session and returns the current Sunday-start operational-week workout count. |
| `/api/intel-books` | GET | Authenticates the session, paginates the complete Archive and Reading Reports, and returns separately sorted Active Materials with latest Book-linked read dates plus the deduplicated union of all eligible Priority Band records and at most five highest-Fit-Score recommendations. |
| `/api/intel-reports` | POST | Authenticates the session, verifies Archive ownership, validates the absolute ending page against Current Page and Total Pages, derives pages read and book identity server-side, then creates or recovers a deterministic Reading Report and updates Current Page. |
| `/api/smu/orders` | GET | Authenticates the session and groups assignments using Denver-midnight query boundaries and Denver date normalization for due-soon and overdue windows. |
| `/api/smu/orders/complete` | POST | Authenticates the session, verifies the supplied page belongs to Assignments, and marks it Complete. |
| `/api/smu/orders/focus` | POST | Authenticates the current session, verifies the target belongs to Assignments, and marks it for the Focus Queue. |
| `/api/smu/pipeline` | GET | Authenticates the session, reads all assignments, and calculates course progress. |
| `/api/smu/quarter` | GET | Authenticates the current session and returns active and up-next records from the Notion Quarters data source. |
| `/api/mobile/hud` | GET | Authenticates the session and returns Daily SITREP values plus hardcoded weekly values. |
| `/api/mobile/hud/objective` | POST | Authenticates the session and updates an allowlisted Daily SITREP objective. |
| `/api/mobile/hydration` | GET, POST | Authenticates the session but still uses volatile module memory; not durable. |
| `/api/mobile/intel` | POST | Authenticates the session, bounds the JSON payload, then logs and echoes it; no durable report is created. |

## Approved but not implemented architecture

- **Academic Operations core:** approved for 0.6.1 as the authoritative domain for real-world courses, assignments, due dates, required or optional status, completion, readiness, recommendations, and academic history. Current SMU code partially implements this domain but does not yet satisfy the approved contract.
- **Specialization School overlay:** planned for 0.9. It will consume private, versioned Academic Operations evidence through a configurable curriculum and Competency Matrix without rewriting source assignments. No specialization overlay, evidence ledger, competency state, privacy enforcement, or qualification handoff exists in the repository.
- **Competency progression:** Introduced, Practiced, Demonstrated, and Qualified are approved product states, not implemented behavior.
- **Multiplayer boundary:** raw academic details remain private by default; shared surfaces will consume derived progress only. A durable multi-user identity model remains unresolved.

See [ADR-0007](adr/0007-academic-operations-and-specialization-overlay.md) for the accepted boundary.

## Deferred work

No feature is explicitly recorded as Deferred in the repository or migration notes. Future deferrals should include a reason, owner, and reconsideration condition.
